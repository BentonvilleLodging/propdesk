[build]
  publish = "."
  functions = "netlify/functions"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Run every day at 8:00 AM UTC (3 AM Central)
[functions."check-due-tasks"]
  schedule = "0 8 * * *"

# get-search-health: schedule temporarily removed for manual testing via
# browser (Netlify blocks direct HTTP access to scheduled functions).
# Re-add once the PriceLabs listing-ID mapping is confirmed:
# [functions."get-search-health"]
#   schedule = "0 11 * * *"
